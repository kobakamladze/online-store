import { Router } from "express";

const deviceRouter = new Router();

deviceRouter.post("/", () => {});
deviceRouter.get("/", () => {});
deviceRouter.get("/:id", () => {});

export default deviceRouter;
